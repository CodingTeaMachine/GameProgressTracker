export enum Pages {
	HOME = '/',
	PROGRESS = '/progress',
	LIBRARY = '/library',
	EXPLORE = '/explore',
	BACKLOG = '/backlog',
	PERSONAL_STATS = '/personal-stats',
	NEW_GAME = '/new-game',
	LOGIN = '/login',
	REGISTER = '/register',
	PROFILE = '/profile'
}

export enum Endpoints {
	LOGOUT = '/logout'
}
